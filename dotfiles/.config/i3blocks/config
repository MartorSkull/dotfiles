# align
# color
# command
# full_text
# instance
# interval
# label
# min_width
# name
# separator
# separator_block_width
# short_text
# signal
# urgent
# Global properties
#
# The top properties below are applied to every block, but can be overridden.
# Each block command defaults to the script name to avoid boilerplate.

command=$HOME/.config/i3blocks/blocks/$BLOCK_NAME
separator_block_width=15
markup=none

[mediaplayer]
interval=1

[volume]
label=
interval=once
signal=10

[wifi]
interval=2
label=
separator=false

[iface]
interval=10
markup=pango

[disk]
interval=2
instance=/
separator=false
label=/

[disk]
interval=2
label=

[cpu_freq]
interval=1
label=
markup=pango

[cpu_usage]
interval=persist
min_width= 99%
markup=pango

[mybattery]
command=~/.config/i3blocks/blocks/mybattery
interval=5

[date]
command=date "+%d-%m-%y"
interval=10
label= 

[time]
command=date "+%H:%M:%S"
interval=1 
label=

[keyindicator]
interval=once
instance=NUM
signal=11

[shutdown_menu]
full_text= 
min_width= 
command=/home/martin/.config/i3blocks/blocks/shutdown_menu && echo ""
